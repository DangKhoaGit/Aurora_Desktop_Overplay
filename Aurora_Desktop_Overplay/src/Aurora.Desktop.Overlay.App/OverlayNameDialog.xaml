<Window x:Class="Aurora.Desktop.Overlay.App.OverlayNameDialog"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="Overlay name" Width="430" MinWidth="380" SizeToContent="Height" ResizeMode="NoResize"
 WindowStartupLocation="CenterOwner" ShowInTaskbar="False" UseLayoutRounding="True" SnapsToDevicePixels="True">
 <Grid Margin="24"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <TextBlock x:Name="HeadingText" FontSize="20" FontWeight="SemiBold" Text="Name your overlay"/>
  <TextBlock Grid.Row="1" Margin="0,7,0,12" Foreground="{DynamicResource MutedTextBrush}"
             TextWrapping="Wrap" Text="Choose a name that is easy to recognize."/>
  <TextBox x:Name="NameTextBox" Grid.Row="2" Height="38" MaxLength="100" Padding="10,7" VerticalContentAlignment="Center"
           Foreground="{DynamicResource TextBrush}" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
  <StackPanel Grid.Row="3" Margin="0,20,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
   <Button Width="90" Height="38" Margin="0,0,8,0" IsCancel="True" Content="Cancel"/>
   <Button Width="90" Height="38" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="ConfirmClick" Content="Save"/>
  </StackPanel>
 </Grid>
</Window>
